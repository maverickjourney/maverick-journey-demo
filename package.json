{
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/index.html && cp slack.html dist/slack.html && cp client.html dist/client.html"
  },
  "dependencies": {},
  "devDependencies": {}
}
